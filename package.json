{
  "name": "mediafire-real-browser-resolver",
  "version": "2.0.0",
  "description": "MediaFire direct link extractor using real browser (Playwright)",
  "main": "server.js",
  "scripts": {
    "preinstall": "rm -f package-lock.json && npm config set package-lock false",
    "install": "npm install --no-package-lock --no-audit",
    "postinstall": "npx playwright install chromium",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.10.0",
    "playwright": "1.40.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
